import { BACKEND_BASE_URL, SPLITWISE_CONSUMER_KEY } from 'react-native-dotenv';

export default {
  api: {
    baseUrl: BACKEND_BASE_URL,
  },
  splitwise: {
    consumerKey: SPLITWISE_CONSUMER_KEY,
  },
};
